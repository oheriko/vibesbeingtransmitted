# App Configuration
APP_URL=https://vibesbeingtransmitted.com
PORT=3000
DATABASE_PATH=./vibes.db

# Encryption key for OAuth tokens (32 bytes = 64 hex characters)
# Generate with: openssl rand -hex 32
ENCRYPTION_KEY=

# Slack App Credentials
# Create app at: https://api.slack.com/apps
# Required Bot Token Scopes: commands, chat:write, users:read
# Required User Token Scopes: users.profile:write, users.profile:read
SLACK_CLIENT_ID=
SLACK_CLIENT_SECRET=
SLACK_SIGNING_SECRET=

# Spotify App Credentials
# Create app at: https://developer.spotify.com/dashboard
SPOTIFY_CLIENT_ID=
SPOTIFY_CLIENT_SECRET=
# Optional: Override Spotify redirect URI (useful for local dev with http://127.0.0.1:3000/auth/spotify)
# SPOTIFY_REDIRECT_URI=http://127.0.0.1:3000/auth/spotify
